{
  "name": "@M324/db",
  "type": "module",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    },
    "./schema/auth": {
      "development": "./src/schema/auth.ts",
      "types": "./dist/schema/auth.d.mts",
      "default": "./dist/schema/auth.mjs"
    }
  },
  "scripts": {
    "build": "tsdown src/index.ts src/schema/auth.ts --format esm --dts --out-dir dist",
    "check-types": "tsc --noEmit",
    "db:push": "drizzle-kit push",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio",
    "db:migrate": "drizzle-kit migrate",
    "db:start": "docker compose --project-directory ../.. up -d",
    "db:watch": "docker compose --project-directory ../.. up",
    "db:stop": "docker compose --project-directory ../.. stop",
    "db:down": "docker compose --project-directory ../.. down"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.13.11",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.20.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "tsdown": "^0.21.10",
    "typescript": "^6"
  }
}
